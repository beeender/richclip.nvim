local CONFIG = {
    --- Specify the richclip executable path. If it is nil, the plugin will try to download the
    --- it automatically.
    richclip_path = nil,
    --- To print debug logs
    enable_debug = false,
}

return CONFIG
